import { CircularProgress as MuiCircularProgress } from "@material-ui/core";
import styled from "styled-components";

export const PageLoad = styled(MuiCircularProgress)`
  margin: 0 auto;
  display: block;
`;
