import styled from "styled-components";

export const Container = styled.div`
  display: grid;
  grid-template-rows: auto;
  padding: 24px;
  background: #eeeded;
  .card {
    padding: 12px;
    background: #fff;
    margin-bottom: 36px;
  }
`;
